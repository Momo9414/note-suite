# Database Configuration
POSTGRES_DB=notes_db
POSTGRES_USER=notes_user
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_PORT=5432

# Database Connection (for backend)
DATABASE_URL=jdbc:postgresql://db:5432/notes_db
DATABASE_USERNAME=notes_user
DATABASE_PASSWORD=change_me_in_production

# JWT Configuration
JWT_SECRET=change_me_to_a_secure_secret_key_with_at_least_256_bits

# Backend API
BACKEND_PORT=8080

# Frontend Web
FRONTEND_PORT=8081

# CORS (comma-separated origins)
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://localhost:8081,http://localhost:3000
